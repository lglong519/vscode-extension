# About
      run the js file which is openning and active.
## Install vsce
      npm install -g vsce
## Packaging extension
      vsce package
## Install extension
      code --install-extension run-0.0.1.vsix